{
  "name": "opencode-checkpoints",
  "description": "OpenCode plugin with automatic git stash checkpoints",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  }
}
